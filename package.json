{
  "name": "ts-module-scaffold",
  "version": "0.0.0",
  "description": "",
  "main": "dist/ts-module-scaffold/index.js",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "link": "npm run build && npm link --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "private",
  "author": "",
  "devDependencies": {
    "@types/node": "^20.5.7",
    "typescript": "^5.2.2"
  }
}
